{
  "name": "coin-flip",
  "version": "1.0.0",
  "description": "A simple, JS-based, heads or tails game.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass-watcher": "npx sass --watch ./sass/styles.scss:./css/styles.css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.62.1"
  }
}
